Let $\tilde{\mathbb{P}}(\,\cdot\,)\coloneqq \mathbb{P}(\,\cdot\,\mid X\ge u)$ and let $Y_u$ denote the random variable
\(
Y_u\coloneqq X-u
\)
under $\tilde{\mathbb{P}}$. Then define:
\(
d(u)=\tilde{\mathbb{E}}[Y_u].
\) 

We want to prove the first few conditional moments of $Y_u$: for $k\geq 0$, 
\[
\mu_k(u):= \tilde{\mathbb{E}}\bigl[(Y_u)^k\bigr]
=\mathbb{E}\bigl[(X-u)^k\mid X\ge u\bigr].
\]
Recall that $d(u)\coloneqq E(u)-u=\mu_1(u)$. A standard integration by parts recursion gives
\begin{align}
\mu_0(u)&=1,\quad 
\mu_1(u)=d(u),\label{eq:moments-TOBECHECKED}\\
\mu_2(u)&=1-u\,d(u),\nonumber\\
\mu_3(u)&=(u^2+2)\,d(u)-u,\nonumber\\
\mu_4(u)&=u^2+3-u(u^2+5)\,d(u).\nonumber
Got it — I've updated the instructions to include full details as a blueprint for a Lean file.


  File: TruncatedNormalMoments.lean

  Goal (blueprint):
    Let X ∼ N(0,1), condition on {X ≥ u}, and let Y_u := X - u.
    Define μ_k(u) := E[(Y_u)^k | X ≥ u] and d(u) := μ_1(u).
    Prove:
      μ_0(u) = 1
      μ_1(u) = d(u)
      μ_2(u) = 1 - u * d(u)
      μ_3(u) = (u^2 + 2) * d(u) - u
      μ_4(u) = u^2 + 3 - u * (u^2 + 5) * d(u)

  Strategy:
    Work on ℝ with Lebesgue measure and the standard normal density φ.
    Define tail(u) = ∫_{x∈Ici u} φ(x) dx and J_k(u) = ∫_{x∈Ici u} (x-u)^k φ(x) dx.
    Then μ_k(u) = J_k(u) / tail(u).

    Prove an integration-by-parts recursion (for k ≥ 1):
      J_{k+1}(u) = (k : ℝ) * J_{k-1}(u) - u * J_k(u).

    Divide by tail(u) = J_0(u) to get the μ-recursion:
      μ_{k+1}(u) = (k : ℝ) * μ_{k-1}(u) - u * μ_k(u).

    Then compute μ_2, μ_3, μ_4 from μ_0 = 1 and μ_1 = d.
-/

